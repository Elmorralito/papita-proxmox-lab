---
description: papita-proxmox-lab repository identity, layout, and configuration map
alwaysApply: true
---

# papita-proxmox-lab — Repository Map

Hybrid **Proxmox VE cluster + AWS** lab. On-prem PVE nodes connect to AWS (EFS shared storage) over **Tailscale**. Bash deploy scripts orchestrate node setup and Terraform; Python tooling is dev-only (Poetry + pre-commit).

## Architecture (high level)

```
Local workstation
  deploy/toolkit.sh ─┬─► deploy/proxmox.sh ──SSH──► PVE nodes (deploy/setup/setup-pve-node.sh)
                     └─► deploy/terraform.sh ──────► terraform/plans/ (S3 backend)
                                                          └─► hybrid_proxmox_aws_cluster
                                                                └─► aws/ (VPC, EFS, IAM)
```

## Directory tree

```
papita-proxmox-lab/
├── deploy/                    # Orchestration entrypoints (run from repo root)
│   ├── toolkit.sh             # Main CLI: build|test|proxmox|terraform + AWS auth
│   ├── proxmox.sh             # SSH to PVE: setup-node, cluster start/stop, get-temp
│   ├── terraform.sh           # init → workspace → plan|apply|destroy
│   ├── utils.sh               # log(), prompts (prompt_pve_start, prompt_until_*), AWS auth, run_command
│   ├── usage.sh               # usage_toolkit, usage_proxmox, usage_terraform, usage_setup_pve_node
│   ├── tailscale-pfsense-lan.sh   # Tailscale ACL + pfSense LAN helper (workstation)
│   ├── docs/
│   │   ├── setup-pve-node.usage.txt       # PVE setup manual (less during prompts)
│   │   └── tailscale-pfsense-lan.usage.txt
│   ├── setup/                  # SCP'd to /root/deploy on PVE by proxmox.sh setup-node
│   │   ├── setup-pve-node.sh      # Interactive 18-step PVE bootstrap (PVE_SETUP_LAST_STEP=18)
│   │   ├── post-startup-proc.sh   # Boot: quorum wait, ceph noout unset, WoL from main node
│   │   ├── pre-shutdown-proc.sh   # Shutdown: optional pvesh stopall, ceph osd set noout
│   │   ├── post-startup-proc.service / pre-shutdown-proc.service
│   │   ├── apt-dependencies.list  # Step 1 packages (jq, lm-sensors, chrony, smartmontools, …)
│   │   ├── misc/tailscale/
│   │   │   ├── default.tags.list      # Tailscale ACL tags (tag:private-node, …)
│   │   │   ├── default.gateways.list  # Optional PVE subnet routes (empty; pfSense advertises LAN)
│   │   │   └── default.lan.routes.list  # Main-node fallback LAN route (172.16.0.0/16)
│   │   └── misc/cluster/              # QDevice + TrueNAS NFS + HA (setup-cluster-ha)
│   │       ├── default.qdevice.host / default.truenas.nfs.env
│   │       ├── qdevice-server-bootstrap.sh
│   │       ├── papita-node-qdevice-client.sh
│   │       └── papita-cluster-quorum-ha.sh
│   └── python/                # SCP'd to /root/deploy/python on PVE (proxmox.sh setup-node)
│       ├── misc/cluster/
│       │   ├── discover_hosts.py      # Step 7: DNS peer discovery → /etc/hosts
│       │   └── domain_pattern.py      # Wildcard domain suffix keywords (oldtimers.*)
│       └── datafiles/
│           ├── default.hosts.list     # Candidate node short names
│           ├── default.hosts.regex    # FQDN filter regex
│           └── default.domain.suffixes.list  # Zone labels for .* suffix expansion
├── terraform/
│   ├── environments/          # NOT in git — config.{dev|prod|poc}.tfvars (see .gitignore)
│   └── plans/
│       ├── main.tf            # Root module; S3 backend; wires hybrid_proxmox_aws_cluster
│       ├── variables.tf       # environment, owner, aws_*, plan_specific_aws_security_params
│       └── hybrid_proxmox_aws_cluster/
│           ├── main.tf        # AWS + Tailscale providers; module aws
│           ├── variables.tf / locals.tf
│           ├── aws/           # VPC (optional), EFS + access point, SG, IAM for Tailscale router
│           │   ├── network.tf, efs.tf, iam.tf, outputs.tf
│           └── tailscale/     # Placeholder module (variables only; no resources yet)
├── docs/
│   ├── TIPSNTRICKS.md         # Ceph, OSD, cluster maintenance runbooks
│   └── Diagrams.drawio
├── rules-tmp/                 # Draft Cursor rules (not active; active rules live in .cursor/rules/)
├── pyproject.toml             # Poetry dev deps + linter config (no app package yet)
├── poetry.lock / .python-version (3.14)
├── .pre-commit-config.yaml
├── .gitignore
└── README.md
```

**Referenced but absent from repo:** `libs/` (Poetry wheels), `tests/` (pytest), `terraform/environments/*.tfvars` (local secrets).

## Key workflows

### Toolkit (`deploy/toolkit.sh`)

| Action | Purpose |
|--------|---------|
| `build` | Build wheels from `libs/` → `dist/` |
| `devsync` | build + pip install wheels |
| `test` | build + pytest with coverage |
| `proxmox` / `deploy_proxmox` | Delegate to `proxmox.sh` |
| `terraform` / `deploy_terraform` | Delegate to `terraform.sh` |

Required: `-e dev|prod`. Optional: `--env-file`, AWS SSO/MFA flags, `--pre-commit`, `--proxmox-action`, `--terraform-action`.

### Proxmox (`deploy/proxmox.sh`)

| Action | Purpose |
|--------|---------|
| `setup-node` | SCP `deploy/setup/` to node, run `setup-pve-node.sh` |
| `setup-cluster-ha` | QDevice client on all nodes + TrueNAS NFS + HA group on entry host |
| `get-temp` | Cluster-wide temperature via `sensors -j` (requires step 5) |
| `start-cluster` | WoL peer nodes via `pvenode wakeonlan` |
| `stop-cluster` | Per-node `pvesh stopall` + hypervisor shutdown |
| `cluster-nodes` / `local-node` | Discovery helpers |

Requires `--ip-address`. SSH: ControlMaster multiplexing; password via `PAPITA_SSH_PASSWORD` or keys. Sources `utils.sh` before local prerequisite checks (`jq`, `ssh`, `scp`).

### PVE setup steps (`setup-pve-node.sh`)

Step skip: `_skip_pve_step N` when `START_FROM_STEP > N`. Menu max: `PVE_SETUP_LAST_STEP` (17).

| Step | Topic |
|------|--------|
| 1 | APT/repos, deps, unattended-upgrades, upgrade cron, optional microcode |
| 2 | Hibernate/sleep off (sysctl, sleep.conf, logind) |
| 3 | Wake-on-LAN (ethtool, wol.service, pvenode wakeonlan MAC) |
| 4 | Locales |
| 5 | lm-sensors (`sensors-detect --auto`; enables `get-temp`) |
| 6 | chrony NTP (configurable servers) |
| 7 | `/etc/hosts` cluster peers via `discover_hosts.py` (pre-`pvecm`) |
| 8 | Tailscale install, sysctl, optional cluster.fw rule, optional NAT masq |
| 9 | `tailscale up` + optional post-init sanity check (9.4) |
| 10 | Post-startup systemd (quorum wait, ceph noout unset, main-node WoL) |
| 11 | Pre-shutdown systemd (optional `pvesh stopall`, ceph noout set) |
| 12 | Email (postfix + `pvesh` mailto/mailfrom) |
| 13 | SMART cron (`papita-smart-scan.sh`) |
| 14 | Enable Proxmox cluster firewall (`cluster.fw` enable: 1) |
| 15 | Remove PVE subscription nag |
| 16 | vzdump cron (`papita-vzdump-all.sh`, configurable schedule) |
| 17 | Tailscale TLS cert for UI :8006 (main node only) |
| 18 | QDevice client (`corosync-qdevice`) + softdog watchdog (HA fencing prep) |

**Node-installed helpers (when steps run):** `/usr/local/sbin/papita-{smart-scan,vzdump-all,tailscale-cert-renew}.sh`, `/etc/default/papita-{post-startup,pre-shutdown}`, `/etc/cron.d/papita-*`.

### Terraform (`deploy/terraform.sh`)

Flow: read `terraform/environments/config.<env>.tfvars` → `terraform init` (S3 backend from tfvars) → workspace `<project>-<env>` → action.

Root entrypoint: **`terraform/plans/main.tf`** (not the hybrid submodule directly).

## Terraform module wiring

```
terraform/plans/main.tf
  └── module hybrid_proxmox_aws_cluster (./hybrid_proxmox_aws_cluster)
        └── module aws (./aws)
              ├── network.tf  — optional VPC/subnets; EFS SG (port 2049 from Tailscale CIDR)
              ├── efs.tf      — encrypted EFS, access point at /pve, KMS optional
              └── iam.tf      — Tailscale router EC2 role (SSM only)
```

**Resource basename:** `{plan_version}-{owner}-{project}-{environment}-{region}` (see `hybrid_proxmox_aws_cluster/locals.tf`).

**Providers:** aws ~> 5.28, tailscale ~> 0.13, archive, null, external, local, random.

**Environments:** `dev`, `prod`, `poc` (validated in `terraform/plans/variables.tf`).

## Configuration files

| File | Role |
|------|------|
| `pyproject.toml` | Poetry (package-mode=false); black/isort/flake8/mypy/pylint/interrogate settings |
| `.pre-commit-config.yaml` | trailing-ws, yaml/toml, shellcheck, isort, black, flake8, pylint, mypy |
| `.gitignore` | `.terraform/`, `*.tfvars`, `.env`, `poetry.lock`, drawio backups |
| `terraform/plans/.terraform.lock.hcl` | Provider lock at plans root |
| `terraform/plans/hybrid_proxmox_aws_cluster/.terraform.lock.hcl` | Provider lock at plan module |
| `terraform/plans/hybrid_proxmox_aws_cluster/aws/.terraform.lock.hcl` | Provider lock at aws submodule |

## Conventions for agents

- **Shell:** `set -euo pipefail`; source `deploy/utils.sh` + `deploy/usage.sh` before calling `log`; use `log INFO|WARN|ERROR` (or `log "INFO" "…"` in deploy scripts).
- **Remote deploy path on PVE:** `/root/deploy` (default).
- **Secrets:** never commit `.tfvars`, `.env`, Tailscale API keys; load via env or `--env-file`.
- **Python:** dev tooling only today; extend `libs/` + `tests/` when adding packages.
- **Terraform changes:** edit under `terraform/plans/`; env-specific values go in untracked `terraform/environments/config.<env>.tfvars`.
- **Docs:** operational runbooks in `docs/TIPSNTRICKS.md`; network diagram in `docs/Diagrams.drawio`.
